Upon running the command 

 ```bash
python -m data_preprocessing.main
```

from the root directory , the original dataset will get downloaded into the data/raw folder

and after a few minutes the cleaned dataset will be downloaded into data/processed folder
